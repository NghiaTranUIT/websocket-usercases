### Websocket Testcase
1. `npm i`
2. Start Proxyman for macOS at port 9090

- Test Websocket (WS): Run `npm run start_ws`
- Test Secure Websocket (WSS): Run `npm run start_wss`
-> Try to run WSS with and without the SSL Proxying.

3. See the websocket UI on the app